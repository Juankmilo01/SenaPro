@model IEnumerable<Senalai.Models.Roles>

@{
    ViewBag.Title = "Index";
}

<h2 style=" text-align:center">ROLES</h2>

<p>
    @Html.ActionLink("Nuevo Rol", "Create", null, new { @class = "btn btn-warning" })
</p>
<table class="table table-light table-hover">
    <thead class="thead-light">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreRoles)
            </th>
            <th>Acciones</th>
        </tr>
    </thead>

    @foreach (var item in Model)
    {
    <tbody>
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreRoles)
            </td>
            <td>
                @Html.ActionLink("Modificar", "Edit", new { id = item.RolesID }, new { @class = "btn btn-success" })

                @Html.ActionLink("Detalles", "Details", new { id = item.RolesID }, new { @class = "btn btn-info" })

                @Html.ActionLink("Borrar", "Delete", new { id = item.RolesID }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    </tbody>
            }

    </table>
