@model IEnumerable<Senalai.Models.Prestamos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ElementosID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ElementosID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PrestamosID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PrestamosID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PrestamosID })
        </td>
    </tr>
}

</table>
