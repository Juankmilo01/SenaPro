@model IEnumerable<Senalai.Models.Tipo_Elementos>

@{
    ViewBag.Title = "Index";
}

<h2 style=" text-align:center">TIPO DE ELEMENTOS</h2>

<p>
    @Html.ActionLink("Nuevo Tipo", "Create", null, new { @class = "btn btn-warning" })
</p>
<table class="table table-light table-hover">
    <thead class="thead-light">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre_TipoElemento)
            </th>
            <th>Acciones</th>
        </tr>
    </thead>

    @foreach (var item in Model)
    {
    <tbody>
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre_TipoElemento)
            </td>
            <td>
                @Html.ActionLink("Modificar", "Edit", new { id = item.Tipo_ElementosID }, new { @class = "btn btn-success" })
  
                @Html.ActionLink("Detalles", "Details", new { id = item.Tipo_ElementosID }, new { @class = "btn btn-info" })
        
                @Html.ActionLink("Borrar", "Delete", new { id = item.Tipo_ElementosID }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    </tbody>
            }

    </table>
