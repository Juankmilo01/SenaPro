@model IEnumerable<Senalai.Models.Tipo_Elementos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_TipoElemento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_TipoElemento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Tipo_ElementosID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Tipo_ElementosID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Tipo_ElementosID })
        </td>
    </tr>
}

</table>
