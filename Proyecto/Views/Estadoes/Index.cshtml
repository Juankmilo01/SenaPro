@model IEnumerable<Senalai.Models.Estado>

@{
    ViewBag.Title = "Index";
}

<h2 style=" text-align:center">ESTADOS</h2>

<p>
    @Html.ActionLink("Nuevo Estado", "Create", null, new { @class = "btn btn-warning" })
</p>
<table class="table table-light table-hover">
    <thead class="thead-light">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreEstado)
            </th>
            <th>Acciones</th>
        </tr>
    </thead>

    @foreach (var item in Model)
    {
    <tbody>
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreEstado)
            </td>
            <td>
                @Html.ActionLink("Modificar", "Edit", new { id = item.EstadoID }, new { @class = "btn btn-success" })

                @Html.ActionLink("Detalles", "Details", new { id = item.EstadoID }, new { @class = "btn btn-info" })

                @Html.ActionLink("Borrar", "Delete", new { id = item.EstadoID }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    </tbody>
            }

    </table>
