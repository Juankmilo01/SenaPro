@model IEnumerable<Senalai.Models.Estado_Elementos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_Estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Estado_ElementosID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Estado_ElementosID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Estado_ElementosID })
        </td>
    </tr>
}

</table>
