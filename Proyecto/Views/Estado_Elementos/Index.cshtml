@model IEnumerable<Senalai.Models.Estado_Elementos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_Estado)
        </th>
        <th></th><th>Acciones</th><th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Estado)
        </td>
        <td>
            @Html.ActionLink("Modificar", "Edit", new { id = item.Estado_ElementosID }, new { @class = "btn btn-success" })
        </td>
        <td>
            @Html.ActionLink("Detalles", "Details", new { id = item.Estado_ElementosID }, new { @class = "btn btn-info" })
        </td>
        <td>
            @Html.ActionLink("Borrar", "Delete", new { id = item.Estado_ElementosID }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
